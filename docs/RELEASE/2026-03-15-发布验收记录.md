# 2026-03-15 发布验收记录

## 1. 本次验收目标

- 验证当前 `main` 分支是否已与远端同步
- 实际触发远端 CI/CD 发布工作流
- 确认质量门禁、镜像构建、镜像推送是否全部成功
- 校验外部镜像仓库标签是否已经可见、可被拉取

## 2. 代码同步结果

- 本地分支：`main`
- 远端分支：`origin/main`
- 执行命令：`git push origin main`
- 返回结果：`Everything up-to-date`

说明：

- 当前本地没有未推送提交
- `HEAD` 已对齐远端 `origin/main`
- 当前远端提交为 `2c9312c73726ac7efa0cab6b499ac920be948704`

## 3. 实际触发的 CI/CD

- 工作流：`Build and Push Docker Image`
- 触发方式：`workflow_dispatch`
- 运行 ID：`23103356939`
- 运行地址：
  - `https://github.com/ZeroPointSix/outlookEmailPlus/actions/runs/23103356939`

### 3.1 工作流结果

- 总结论：`success`

### 3.2 Job 结果

- `quality-gate`：`success`
- `build-and-push`：`success`

### 3.3 quality-gate 明细

以下步骤全部成功：

- `Install dependencies`
- `Run formatter checks`
- `Run static analysis gate`
- `Run security gate`
- `Run publish gate tests`

### 3.4 build-and-push 明细

以下步骤全部成功：

- `Set up Docker Buildx`
- `Compute image names`
- `Log in to GHCR`
- `Log in to Docker Hub (optional)`
- `Extract metadata`
- `Build and push Docker image`
- `Image digest`

## 4. 镜像推送结果

### 4.1 GHCR

- 仓库：`ghcr.io/zeropointsix/outlook-email-plus`
- 标签：
  - `main`
  - `latest`
  - `main-2c9312c`

### 4.2 Docker Hub

- 仓库：`guangshanshui/outlook-email-plus`
- 标签：
  - `main`
  - `latest`
  - `main-2c9312c`

### 4.3 本次发布 digest

- Manifest list digest：
  - `sha256:0c5e2487f442b5c981f96b923fa010fd2d396510d9f8bcb7d714701eb6033784`

### 4.4 多架构结果

远端 manifest 检查确认已包含：

- `linux/amd64`
- `linux/arm64`

对应子 manifest：

- `sha256:958ac49c8c6f4e1994ac3c5a8646fc3fa20b7477a458b2b8688c0a823bc9fcf0` (`amd64`)
- `sha256:18d690a3146fc533afbfd1296f841ad1f6f7e2f7f6bc932a06fb22c1732d2fc3` (`arm64`)

## 5. 外部可见性校验

### 5.1 已执行的校验

- 执行了远端 workflow 触发与运行状态读取
- 执行了 GHCR / Docker Hub 的远端 manifest 检查
- 执行了 `docker pull ghcr.io/zeropointsix/outlook-email-plus:latest`
- 执行了 `docker pull guangshanshui/outlook-email-plus:latest`

### 5.2 结论

- GHCR `latest` 标签已对外可见
- Docker Hub `latest` 标签已对外可见
- 两个仓库都能返回有效 OCI image index
- 说明镜像已经实际推送成功，外部拉取链路成立

补充说明：

- 本机 `docker pull` 在最后几层下载阶段回显较慢，但远端 manifest 已成功返回
- 且 workflow 日志已明确记录 `pushing manifest` 成功到 GHCR 与 Docker Hub
- 因此可判定这次发布不是“只构建未推送”，而是“已成功推送并可被外部访问”

## 6. 发布时间点

- 本地记录时间：`2026-03-15 12:52:07 +08:00`
- 远端 workflow 完成时间（UTC）：`2026-03-15T04:32:03Z`
- 对应北京时间：`2026-03-15 12:32:03 +08:00`

## 7. 发布结论

本次发布验收通过。

当前仓库已满足以下条件：

- 代码已与远端同步
- 远端 CI/CD 可正常触发
- 质量门禁通过
- 多架构镜像构建成功
- GHCR 推送成功
- Docker Hub 推送成功
- 外部镜像标签已可见

当前可以按“受控私有接入的邮箱读取 API 服务”口径继续发布与交付。
